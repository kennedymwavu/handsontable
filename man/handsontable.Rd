% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handsontable.R
\name{handsontable}
\alias{handsontable}
\title{Create a Handsontable Widget}
\usage{
handsontable(
  data,
  width = NULL,
  height = NULL,
  adaptiveHeight = TRUE,
  colHeaders = TRUE,
  rowHeaders = TRUE,
  readOnly = FALSE,
  contextMenu = FALSE,
  elementId = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame, matrix, or other rectangular data structure}

\item{width, height}{Widget dimensions. Can be specified as pixels
(e.g., "400px") or percentage (e.g., "100%"). If NULL, dimensions are
automatically determined.}

\item{adaptiveHeight}{Logical. If TRUE (default), the table's height
will automatically adjust based on the number of rows displayed. If FALSE,
the specified height will be used.}

\item{colHeaders}{Logical or character vector. If TRUE, shows default
column headers.
If character vector, uses custom column names.}

\item{rowHeaders}{Logical. If TRUE, shows row numbers as headers.}

\item{readOnly}{Logical. If TRUE, makes the entire table read-only.}

\item{contextMenu}{Logical or list. If TRUE, enables default context menu.
If list, provides custom context menu configuration.}

\item{elementId}{Optional HTML element ID for the widget container}

\item{...}{Additional configuration options passed to Handsontable}
}
\value{
An htmlwidget object for rendering the Handsontable
}
\description{
Creates an interactive data grid using the Handsontable JavaScript library.
The widget provides spreadsheet-like functionality including data validation,
conditional formatting, formulas, and extensive customization options.
}
\examples{
if (interactive()) {
  # Basic usage
  handsontable(mtcars)

  # With custom headers
  handsontable(iris, colHeaders = c(
    "S.Length", "S.Width", "P.Length",
    "P.Width", "Species"
  ))

  # Read-only table with context menu
  handsontable(airquality, readOnly = TRUE, contextMenu = TRUE)

  # Using pipe operator for configuration
  handsontable(mtcars, contextMenu = TRUE) |>
    hot_cols(colWidths = 100)
}

}
